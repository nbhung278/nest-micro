   syntax = "proto3";

   package ai;

   service AiService {
     rpc GetReply (AiRequest) returns (AiReply);
   }

   message AiRequest {
     string message = 1;
   }

   message AiReply {
     string reply = 1;
   }